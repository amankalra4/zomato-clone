import CircularProgress from "@material-ui/core/CircularProgress";

const CircularLoader = () => (
    <CircularProgress color="primary" style={{ display: "flex", justifyContent: "center", margin: "0 auto" }} />
);

export default CircularLoader;
