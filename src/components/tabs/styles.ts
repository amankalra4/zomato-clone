import { css } from "@emotion/css";

const commonHeading = css`
  color: rgb(28, 28, 28);
  font-size: 2rem;
  margin: 2.2rem 0;
`;

const circularProgress = css`
  display: flex !important;
  margin: 20px auto !important;
`;

const tabImage = css`
  aspect-ratio: 1 / 1;
`;

export { commonHeading, circularProgress, tabImage };
